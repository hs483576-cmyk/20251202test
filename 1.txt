<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>植物的運輸 - 維管束的功能與分布</title>
    <style>
        :root {
            --primary-green: #2e7d32;
            --light-green: #e8f5e9;
            --highlight-blue: #1565c0; /* 水分、木質部相關 */
            --highlight-orange: #ef6c00; /* 養分、韌皮部相關 */
            --correct-bg: #d1e7dd;
            --correct-border: #0f5132;
            --correct-text: #0f5132;
            --incorrect-bg: #f8d7da;
            --incorrect-border: #842029;
            --incorrect-text: #842029;
        }

        body {
            font-family: "Microsoft JhengHei", "Heiti TC", sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f7f6;
            color: #333;
            line-height: 1.6;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
            background: #fff;
            min-height: 100vh;
            box-shadow: 0 0 15px rgba(0,0,0,0.05);
        }

        header {
            text-align: center;
            padding-bottom: 20px;
            border-bottom: 2px solid #eee;
            margin-bottom: 30px;
        }

        h1 {
            color: var(--primary-green);
            margin-bottom: 5px;
        }

        h2 {
            border-left: 5px solid var(--primary-green);
            padding-left: 15px;
            margin-top: 30px;
            color: #444;
        }

        /* 文本樣式與關鍵字高亮 */
        .content-text {
            font-size: 1.1rem;
            text-align: justify;
            background: #fafafa;
            padding: 20px;
            border-radius: 10px;
        }

        .kw-blue {
            color: var(--highlight-blue);
            font-weight: bold;
        }

        .kw-orange {
            color: var(--highlight-orange);
            font-weight: bold;
        }

        /* 圖片區域 */
        .image-section {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin: 30px 0;
        }

        .image-card {
            flex: 1;
            min-width: 300px;
            text-align: center;
            background: #fff;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 8px;
        }

        .image-card img {
            max-width: 100%;
            height: auto;
            border-radius: 5px;
        }

        .image-caption {
            margin-top: 8px;
            font-size: 0.9rem;
            color: #666;
        }

        /* 互動練習區塊 */
        .quiz-section {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 2px dashed #ccc;
        }

        .quiz-title {
            text-align: center;
            color: var(--primary-green);
            margin-bottom: 20px;
        }

        .question-card {
            background: #fff;
            border: 1px solid #ddd;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            transition: all 0.3s ease;
        }

        .question-card.correct {
            background-color: var(--correct-bg);
            border-color: var(--correct-border);
        }

        .question-card.incorrect {
            background-color: var(--incorrect-bg);
            border-color: var(--incorrect-border);
        }

        .q-text {
            font-weight: bold;
            margin-bottom: 15px;
            font-size: 1.05rem;
        }

        .options label {
            display: block;
            margin-bottom: 8px;
            cursor: pointer;
            padding: 8px;
            border-radius: 5px;
        }

        .options label:hover {
            background-color: #f0f0f0;
        }

        /* 按鈕樣式 */
        .btn {
            padding: 8px 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
            transition: background 0.3s;
            margin-top: 10px;
        }

        .btn-check {
            background-color: var(--primary-green);
            color: white;
        }

        .btn-check:disabled {
            background-color: #ccc;
            cursor: not-allowed;
        }

        .btn-retry {
            background-color: #d32f2f;
            color: white;
            display: none; /* 預設隱藏 */
        }

        .feedback-msg {
            margin-top: 10px;
            font-weight: bold;
            display: none;
        }

        .feedback-msg.show {
            display: block;
        }

        /* 導覽列 */
        .nav-footer {
            display: flex;
            justify-content: space-between;
            margin-top: 50px;
            padding-top: 20px;
            border-top: 1px solid #eee;
        }

        .nav-btn {
            padding: 12px 24px;
            text-decoration: none;
            color: white;
            border-radius: 30px;
            font-weight: bold;
            transition: all 0.3s;
        }

        .nav-prev {
            background-color: #757575;
        }

        .nav-prev:hover {
            background-color: #616161;
        }

        .nav-next {
            background-color: var(--primary-green);
        }

        .nav-next.disabled {
            background-color: #bdbdbd;
            pointer-events: none; /* 禁止點擊 */
            cursor: default;
        }

        .nav-next:not(.disabled):hover {
            background-color: #1b5e20;
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
        }

        /* 下拉選單樣式 */
        select {
            padding: 5px;
            font-size: 1rem;
            margin: 0 5px;
        }

    </style>
</head>
<body>

<div class="container">
    <header>
        <h1>植物的運輸探索之旅</h1>
        <p>學習頁面：維管束的功能與分布</p>
    </header>

    <section>
        <h2>維管束的功能</h2>
        <div class="content-text">
            在植物體內有負責運輸<span class="kw-blue">水分</span>及<span class="kw-orange">養分</span>的細胞，排列成束，稱為<span class="kw-blue">維管束</span>。維管束內主要負責運輸水分和礦物質的構造稱為<span class="kw-blue">木質部</span>，其細胞壁比較厚，也具有<span class="kw-blue">支持</span>的功能；而光合作用所製造的養分則由<span class="kw-orange">韌皮部</span>負責運輸。
        </div>
    </section>

    <section>
        <h2>維管束的分布</h2>
        <div class="content-text">
            維管束的組織細胞形狀細長，上下相接形成管狀構造，由根、莖延伸到葉、花和果實。植物的葉脈即是葉內的維管束，葉脈中的<span class="kw-blue">木質部在上側</span>，靠近上表皮；<span class="kw-orange">韌皮部在下側</span>，靠近下表皮。而植物莖內的維管束中，通常<span class="kw-blue">木質部分布在內側</span>，<span class="kw-orange">韌皮部則分布在外側</span>。
        </div>
        
        <div class="image-section">
            <div class="image-card">
                <img src="11.png" alt="葉的維管束構造示意圖">
                <div class="image-caption">圖一：葉的維管束 (木質部在上，韌皮部在下)</div>
            </div>
            <div class="image-card">
                <img src="111.png" alt="莖的維管束構造示意圖">
                <div class="image-caption">圖二：莖的維管束 (木質部在內，韌皮部在外)</div>
            </div>
        </div>
    </section>

    <section class="quiz-section">
        <h3 class="quiz-title">🧠 腦力激盪：隨堂小測驗</h3>
        <p style="text-align:center; margin-bottom:20px; color:#666;">請完成下列四題，全對後即可解鎖下一頁！</p>

        <div class="question-card" id="q1">
            <div class="q-text">1. 植物體內主要負責運輸「水分」與提供「支持」功能的是哪一個構造？</div>
            <div class="options">
                <label><input type="radio" name="q1" value="A" onchange="enableCheck('btn-q1')"> A. 韌皮部</label>
                <label><input type="radio" name="q1" value="B" onchange="enableCheck('btn-q1')"> B. 木質部</label>
                <label><input type="radio" name="q1" value="C" onchange="enableCheck('btn-q1')"> C. 表皮細胞</label>
            </div>
            <button class="btn btn-check" id="btn-q1" onclick="checkAnswer('q1', 'B')" disabled>檢查答案</button>
            <button class="btn btn-retry" id="retry-q1" onclick="retryQuestion('q1')">重做本題</button>
            <div class="feedback-msg" id="fb-q1"></div>
        </div>

        <div class="question-card" id="q2">
            <div class="q-text">2. (是非題) 在植物的「葉脈」中，韌皮部通常位於靠近「上表皮」的上側。</div>
            <div class="options">
                <label><input type="radio" name="q2" value="T" onchange="enableCheck('btn-q2')"> O (正確)</label>
                <label><input type="radio" name="q2" value="F" onchange="enableCheck('btn-q2')"> X (錯誤)</label>
            </div>
            <button class="btn btn-check" id="btn-q2" onclick="checkAnswer('q2', 'F')" disabled>檢查答案</button>
            <button class="btn btn-retry" id="retry-q2" onclick="retryQuestion('q2')">重做本題</button>
            <div class="feedback-msg" id="fb-q2"></div>
        </div>

        <div class="question-card" id="q3">
            <div class="q-text">
                3. 請完成句子：在植物「莖」的維管束中，負責運輸養分的韌皮部分布在 
                <select id="q3-select" onchange="enableCheck('btn-q3')">
                    <option value="">--請選擇--</option>
                    <option value="inner">內側</option>
                    <option value="outer">外側</option>
                </select>。
            </div>
            <button class="btn btn-check" id="btn-q3" onclick="checkSelect('q3', 'outer')" disabled>檢查答案</button>
            <button class="btn btn-retry" id="retry-q3" onclick="retryQuestion('q3')">重做本題</button>
            <div class="feedback-msg" id="fb-q3"></div>
        </div>

        <div class="question-card" id="q4">
            <div class="q-text">4. 如果將植物的莖橫切觀察，發現外圈有一層構造受損導致根部吸收的水分無法運送到葉子，請問受損的最可能是哪一個部位？(提示：請回想位置與功能)</div>
            <div class="options">
                <label><input type="radio" name="q4" value="A" onchange="enableCheck('btn-q4')"> A. 外側的韌皮部</label>
                <label><input type="radio" name="q4" value="B" onchange="enableCheck('btn-q4')"> B. 內側的木質部</label>
            </div>
            <button class="btn btn-check" id="btn-q4" onclick="checkAnswer('q4', 'B')" disabled>檢查答案</button>
            <button class="btn btn-retry" id="retry-q4" onclick="retryQuestion('q4')">重做本題</button>
            <div class="feedback-msg" id="fb-q4"></div>
        </div>

    </section>

    <footer class="nav-footer">
        <a href="index.html" class="nav-btn nav-prev">← 上一頁</a>
        <a href="./2.html" id="next-link" class="nav-btn nav-next disabled" onclick="if(this.classList.contains('disabled')) return false;">下一頁 →</a>
    </footer>
</div>

<script>
    // 狀態追蹤：紀錄每一題是否答對
    const quizStatus = {
        q1: false,
        q2: false,
        q3: false,
        q4: false
    };

    // 啟用檢查按鈕 (當使用者有選擇時)
    function enableCheck(btnId) {
        document.getElementById(btnId).disabled = false;
    }

    // 檢查全域狀態，決定是否解鎖下一頁
    function updateNextButton() {
        const allCorrect = Object.values(quizStatus).every(status => status === true);
        const nextBtn = document.getElementById('next-link');
        
        if (allCorrect) {
            nextBtn.classList.remove('disabled');
            // 這裡可以加入一個簡單的動畫或提示
        } else {
            nextBtn.classList.add('disabled');
        }
    }

    // 處理單選/是非題的檢查邏輯
    function checkAnswer(qId, correctValue) {
        const radios = document.getElementsByName(qId);
        let userVal = null;
        for (let radio of radios) {
            if (radio.checked) {
                userVal = radio.value;
                break;
            }
        }

        const card = document.getElementById(qId);
        const fb = document.getElementById('fb-' + qId);
        const checkBtn = document.getElementById('btn-' + qId);
        const retryBtn = document.getElementById('retry-' + qId);

        if (userVal === correctValue) {
            // 答對
            card.classList.add('correct');
            card.classList.remove('incorrect');
            fb.textContent = "🎉 正確！做得好！";
            fb.style.color = "#0f5132";
            fb.classList.add('show');
            
            // 鎖定按鈕與輸入
            checkBtn.style.display = 'none'; // 隱藏檢查按鈕
            retryBtn.style.display = 'none'; 
            disableInputs(qId, true);
            
            quizStatus[qId] = true;
        } else {
            // 答錯
            card.classList.add('incorrect');
            card.classList.remove('correct');
            fb.textContent = "❌ 答案不對喔，請再讀一次上面的文章試試看。";
            fb.style.color = "#842029";
            fb.classList.add('show');
            
            checkBtn.style.display = 'none';
            retryBtn.style.display = 'inline-block'; // 顯示重試按鈕
            disableInputs(qId, true); // 先鎖定，等按重試再解鎖
            
            quizStatus[qId] = false;
        }
        updateNextButton();
    }

    // 處理下拉選單的檢查邏輯
    function checkSelect(qId, correctValue) {
        const select = document.getElementById(qId + '-select');
        const userVal = select.value;
        
        const card = document.getElementById(qId);
        const fb = document.getElementById('fb-' + qId);
        const checkBtn = document.getElementById('btn-' + qId);
        const retryBtn = document.getElementById('retry-' + qId);

        if (userVal === correctValue) {
            // 答對
            card.classList.add('correct');
            card.classList.remove('incorrect');
            fb.textContent = "🎉 正確！莖的韌皮部在外側。";
            fb.style.color = "#0f5132";
            fb.classList.add('show');
            
            checkBtn.style.display = 'none';
            retryBtn.style.display = 'none';
            select.disabled = true;
            
            quizStatus[qId] = true;
        } else {
            // 答錯
            card.classList.add('incorrect');
            card.classList.remove('correct');
            fb.textContent = "❌ 想想看，莖的構造是「內木外韌」喔！";
            fb.style.color = "#842029";
            fb.classList.add('show');
            
            checkBtn.style.display = 'none';
            retryBtn.style.display = 'inline-block';
            select.disabled = true;
            
            quizStatus[qId] = false;
        }
        updateNextButton();
    }

    // 重做題目功能
    function retryQuestion(qId) {
        const card = document.getElementById(qId);
        const fb = document.getElementById('fb-' + qId);
        const checkBtn = document.getElementById('btn-' + qId);
        const retryBtn = document.getElementById('retry-' + qId);
        
        // 重置樣式
        card.classList.remove('correct', 'incorrect');
        fb.classList.remove('show');
        
        // 重置按鈕
        retryBtn.style.display = 'none';
        checkBtn.style.display = 'inline-block';
        checkBtn.disabled = true; // 需重新選擇才能點擊
        
        // 重置輸入
        if (qId === 'q3') {
            const select = document.getElementById(qId + '-select');
            select.value = "";
            select.disabled = false;
        } else {
            const radios = document.getElementsByName(qId);
            for (let radio of radios) {
                radio.checked = false;
            }
            disableInputs(qId, false);
        }
        
        // 更新狀態
        quizStatus[qId] = false;
        updateNextButton();
    }

    // 輔助函式：鎖定/解鎖 radio inputs
    function disableInputs(name, disabled) {
        const inputs = document.getElementsByName(name);
        for (let input of inputs) {
            input.disabled = disabled;
        }
    }

</script>

</body>
</html>