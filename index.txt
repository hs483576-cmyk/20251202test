<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>植物的運輸探索之旅</title>
    <style>
        /* CSS 樣式設定 */
        body, html {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
            overflow: hidden; /* 防止出現捲軸 */
            font-family: "Microsoft JhengHei", "Heiti TC", sans-serif; /* 微軟正黑體 */
            background: linear-gradient(135deg, #e0f7fa 0%, #f1f8e9 100%); /* 柔和的大自然漸層背景 */
        }

        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
        }

        h1 {
            color: #2e7d32; /* 深綠色主標 */
            font-size: 3.5rem;
            margin-bottom: 0.5rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }

        p.subtitle {
            color: #558b2f; /* 淺綠色副標 */
            font-size: 1.5rem;
            margin-top: 0;
            margin-bottom: 3rem;
            font-weight: 300;
        }

        .img-btn-wrapper {
            position: relative;
            cursor: pointer;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border-radius: 20px;
        }

        .img-btn-wrapper:hover {
            transform: scale(1.05); /* 滑鼠懸停時放大 */
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
        }

        .img-btn-wrapper:active {
            transform: scale(0.98); /* 點擊時微縮 */
        }

        /* 圖片樣式 */
        #start-img {
            max-width: 80vw;
            max-height: 50vh;
            border-radius: 20px;
            display: block;
            border: 5px solid #fff; /* 白色邊框 */
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }
        
        /* 針對無障礙操作的 Focus 樣式 */
        a:focus {
            outline: none;
        }
        a:focus .img-btn-wrapper {
            box-shadow: 0 0 0 4px rgba(46, 125, 50, 0.5);
        }

    </style>
</head>
<body>

    <div class="container">
        <h1>植物的運輸探索之旅</h1>
        
        <p class="subtitle">揭開維管束與水分養分輸送的奧秘</p>

        <a href="1.html" id="link-btn">
            <div class="img-btn-wrapper">
                <img src="0.png" alt="點擊開始探索" id="start-img">
            </div>
        </a>
    </div>

    <script>
        // JavaScript 邏輯控制
        
        // 1. 取得連結元素
        const linkBtn = document.getElementById('link-btn');

        // 2. 監聽全域鍵盤事件
        document.addEventListener('keydown', function(event) {
            // 判斷是否按下 Enter 或 Space (空白鍵)
            if (event.key === 'Enter' || event.key === ' ') {
                // 防止空白鍵造成頁面滾動的預設行為
                event.preventDefault();
                
                // 模擬點擊連結
                linkBtn.click();
            }
        });
    </script>
</body>
</html>